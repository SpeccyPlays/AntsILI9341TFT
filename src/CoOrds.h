#ifndef GSTRUCT_H   /* Include guard */
#define GSTRUCT_H
//just because the Co-ordinates struct is used everywhere
typedef struct {
    int16_t x;
    int16_t y;
} CoOrds;

typedef struct {
    int32_t x;
    int32_t y;
} BigCoOrds;

#endif